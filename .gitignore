# Node modules and build output
node_modules/
dist/

# Editor and system files
.vscode/
.history/
.DS_Store

# Package manager lock files (choose based on your workflow)
package-lock.json
pnpm-lock.yaml

# Ignore everything else if needed
# (Optional, not necessary if plugin root is the Git root)